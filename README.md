نحن نستعمل sqlite كقاعدة بيانات ، ولانشاء الجدول فيها يجب ان نتبع الاتي

شغل الامر التالي قبل كل شئ لانشاء قاعدة البيانات

```php init.php```

ثم شغل السيرفر من خلال الامر التالي

```php -S localhost:8000 -t public```

ذهب الى المتصفح وافتح الرابط التالي 

```localhost:8000/posts```